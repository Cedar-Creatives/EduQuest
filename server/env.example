# Server Configuration
PORT=5000
NODE_ENV=development

# Required API Keys (for AI features)
OPENROUTER_API_KEY=your_openrouter_key_here

# Note: Firebase authentication is handled on the client-side
# No Firebase Admin SDK credentials are required
# The server uses mock implementations for compatibility
